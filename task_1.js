const name = "Protective field generator";
let price = 1000;


console.log( `selected ${name} , price per one ${price} credits`);

let price = 2000;

console.log( `selected ${name} , price per one ${price} credits`);